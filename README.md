<div align="center">

# Metodichka Site

Мультимедийная веб‑версия учебной методички на **React + Vite**: главы, оглавление, медиа (изображения/видео/аудио), интерактивные плашки, быстрый поиск и удобный режим чтения.

</div>

---

## Что здесь есть (фичи)

### Навигация и чтение
- **Оглавление** слева + активная глава
- **Быстрый поиск по главам** в хедере
- **Deep‑link**: ссылка на главу через `#chapter7` + запоминание последней открытой главы
- **Восстановление скролла** после перезагрузки (отдельно для каждой главы)
- **Кнопка “Наверх”**
- **Режим “Фокус”** (прячет сайдбар) + горячая клавиша **F**
- **Примерное время чтения** текущей главы (в хедере)

### Медиа
- Изображения/видео/аудио внутри главы
- **Слайдер “до/после”** для сравнения изображений
- **Лайтбокс**: клик по картинке → просмотр в модальном окне (Esc закрывает)
- **Скачать/Открыть** для видео и аудио (один клик)

### Удобства
- **Toast‑уведомления** (например при копировании ссылки)
- **Копировать ссылку** на текущую главу (кнопка 🔗)
- **Печать** текущей главы (кнопка 🖨) + аккуратные стили для печати
- **Справка (кнопка “?”)** + горячие клавиши
- **Избранное (★)** и **Недавние** главы в сайдбаре
- **Настройки чтения (⚙)**:
  - размер текста
  - межстрочный интервал
  - ширина контента
  - включение/выключение плашек
  - reduce motion (уменьшить анимации)

### Доступность (a11y)
- **Skip to content** (появляется при Tab)

---

## Горячие клавиши

- **N** — следующая глава
- **P** — предыдущая глава
- **/** — фокус в поиск по главам
- **F** — режим фокуса (скрыть/показать сайдбар)
- **Esc** — закрыть модалки (просмотр картинок/справка)
- **?** или **H** — открыть справку

---

## Технологии

- **React**
- **Vite**
- **CSS** (без UI‑библиотек)

---

## Быстрый старт (локально)

Требования:
- Node.js **18+** (лучше LTS)
- Git

Установка:

```bash
npm install
```

Запуск dev‑сервера:

```bash
npm run dev
```

Сборка:

```bash
npm run build
```

Предпросмотр сборки:

```bash
npm run preview
```

---

## Где лежит контент

### Главы/блоки
- Основной контент сейчас находится в **`src/App.jsx`** в массиве `sections`.
- Каждая глава — это объект:
  - `id` (например `chapter7`)
  - `title`
  - `short` (короткое описание)
  - `blocks` (массив блоков)

Поддерживаемые типы блоков (в текущей реализации):
- `text` — JSX‑контент
- `media` — `image | video | audio | hover-image | image-comparison | font-card`
- `quiz` — мини‑тест
- `code` — блок кода (если используется)

### Медиафайлы
- Кладём медиа в: **`public/media/`**
- В коде все пути должны идти через `withBase("/media/...")`, чтобы корректно работало и локально, и на GitHub Pages.

---

## Деплой на GitHub Pages

В проекте есть два пути:

### Вариант A — через `gh-pages` (самый простой)

1) Убедись, что установлен `gh-pages` (в проекте он уже есть в `devDependencies`)
2) Выполни:

```bash
npm run deploy
```

Это соберёт проект и запушит `dist` в ветку `gh-pages`.

Далее на GitHub:
- **Settings → Pages**
- Source: **Deploy from a branch**
- Branch: **gh-pages** / root

### Вариант B — GitHub Actions

Если будешь добавлять workflow:
- поставь `base` в `vite.config.js` либо `./` (универсально), либо `/<repo>/` (жёстко под репо)

---

## Важное про `base` в Vite (чтобы не было “чёрного экрана”/404)

Файл: **`vite.config.js`**

- `base: "./"` — ассеты становятся относительными, почти всегда работает для GitHub Pages в подпапке репозитория
- `base: "/metodichka-site/"` — если хочешь строгий путь и знаешь имя репозитория

Если ассеты (JS/CSS/картинки) не грузятся на Pages — 99% проблема в `base`.

---

## Авто‑коммиты

Скрипт:
- `npm run ac "сообщение"`

Он делает:
- `git add .`
- `git commit -m "...“`
- `git push --force origin main`

Важно: используется **force push** (аккуратно, если работаешь не один).

---

## Траблшутинг

### “На GitHub Pages белый/чёрный экран”
Проверь:
- `vite.config.js` → `base`
- что медиа пути идут через `withBase("/media/...")`

### “Видео не проигрывается автоматически”
Это норм: браузер часто блокирует autoplay со звуком. Для этого в UI есть кнопка play/pause и громкость.

---

## Лицензия

Внутренний учебный проект. Если будешь публиковать публично — проверь права на медиа/тексты.
