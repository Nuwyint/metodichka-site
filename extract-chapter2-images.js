// Скрипт для извлечения изображений из PDF для главы 2
// Использует описание изображений для создания инструкций

import fs from 'fs';
import path from 'path';
import { fileURLToPath } from 'url';

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);

const mediaDir = path.join(__dirname, 'public', 'media');

// Изображения для главы 2
const chapter2Images = {
  'interface-map.png': {
    description: 'Карта интерфейса - UI компоненты и экраны приложения (логин форма, профиль, меню, графики, чат, календарь)',
    figure: 'Рис. 8',
    alt: 'Примеры карт интерфейса'
  },
  'flat-design.png': {
    description: 'Плоский дизайн - мобильные экраны приложения с карточками и UI элементами',
    figure: 'Рис. 9',
    alt: 'Пример плоского дизайна'
  },
  'material-design.png': {
    description: 'Материальный дизайн - UI элементы на фиолетовом фоне (смартфон, карточки, часы, виджет погоды)',
    figure: 'Рис. 10',
    alt: 'Пример материального дизайна'
  },
  'minimal-layout.png': {
    description: 'Минималистичный макет - сравнение хорошего и плохого дизайна карточки "Mountain retreat"',
    figure: 'Рис. 11',
    alt: 'Минималистичный и интуитивный макет'
  },
  'pictograms.png': {
    description: 'Пиктограммы - сетка из 60 минималистичных иконок (монитор, карандаш, папка, корзина, настройки, глаз, самолет, облако, камера, книга, мегафон и т.д.)',
    figure: 'Рис. 12',
    alt: 'Пиктограммы часто используемых операций'
  },
  'responsive-design.png': {
    description: 'Адаптивный дизайн - ноутбук, планшет и смартфон с адаптированным интерфейсом на персиковом фоне',
    figure: 'Рис. 13',
    alt: 'Пример отображения GUI на разных устройствах'
  }
};

console.log('📋 Изображения для главы 2:\n');
console.log('═'.repeat(60));

Object.entries(chapter2Images).forEach(([filename, info]) => {
  const filePath = path.join(mediaDir, filename);
  const exists = fs.existsSync(filePath);
  const status = exists ? '✅' : '❌';
  
  console.log(`\n${status} ${filename}`);
  console.log(`   ${info.figure}: ${info.alt}`);
  console.log(`   Описание: ${info.description}`);
  if (!exists) {
    console.log(`   ⚠️  Файл отсутствует в ${mediaDir}`);
  }
});

console.log('\n' + '═'.repeat(60));
console.log('\n💡 Инструкция по добавлению изображений:');
console.log('1. Откройте PDF файл методички');
console.log('2. Найдите соответствующие рисунки (Рис. 8-13)');
console.log('3. Экспортируйте или сделайте скриншоты');
console.log('4. Сохраните в папку: public/media/');
console.log('5. Используйте точные имена файлов из списка выше\n');

